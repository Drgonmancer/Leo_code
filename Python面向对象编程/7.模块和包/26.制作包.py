"""
二、包
包将有联系的模块组织在一起。即放到同一个文件夹下，并且在这个文件夹创建一个名字为_init_.py文件，那么这个文件夹就称之为包

2.1 制作包
[New]——[Python Package]——输入包名——[OK]——新建功能模块（有联系的模块）
Ps：新建包后，包内部会自动创建_init_.py文件，这个文件控制着包的导入行为

2.1.1 快速体验
1.新建包 mypackage
2.新建包内模块：my_module1和my_module2
3.模块内代码如下：
print(1)


def info_print1():
    print('my_module1')

"""